

<td><%= pet.gender %></td>
<td><% pet.age %> <%= 'an'.pluralize(pet.age) %></td>
<td><%= pet.species.name %></td>
<td>
  <%= link_to 'Voir', pet, class:'btn btn-default' %>
  <%= link_to'Editer', [:edit, :admin, species], class: 'btn btn-primary' %>
  <%= link_to 'Supprimer', [:admin, species], class: 'btn btn-danger', data: {method: :delete, confirm: } %>
</td>
</tr>
<% end %>
</tbody>
</table>
<% end %>

<% render template: 'layouts/profile' %>


